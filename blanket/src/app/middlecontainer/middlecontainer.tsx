import "./middlecontainer.css";

const Middlecontainer = ({ children }: { children: React.ReactNode }) => {
  return <div className="middlecontainer">{children}</div>;
};

export default Middlecontainer;
